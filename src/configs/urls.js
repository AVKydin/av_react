const baseURL = 'https://jsonplaceholder.typicode.com/';

const urls = {
    todos: '/todos',
    albums: '/albums',
    comments: '/comments',
    postComment: '/posts/'
}

export {
    baseURL,
    urls
}