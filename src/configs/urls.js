const baseURL = 'http://owu.linkpc.net/carsAPI/v1';
const baseUsersURL = 'https://jsonplaceholder.typicode.com';

const urls = {
    cars: '/cars',
    users: '/users',
    comments: '/comments'
}

export {
    baseURL,
    baseUsersURL,
    urls
}